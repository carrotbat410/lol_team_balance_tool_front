# Implementer task template

Role contract: `@.codex/agents/implementer.toml`

Use only the canonical task envelope below and the human-approved scope supplied by the Coordinator. Stop if implementation requires an unapproved scope expansion.

```json
{{canonical_envelope}}
```
