# Reviewer task template

Role contract: `@.codex/agents/reviewer.toml`

Use only the canonical task envelope below and the blind Reviewer packet supplied by the Coordinator. Do not request implementation history or runtime logs.

```json
{{canonical_envelope}}
```
