# Researcher task template

Role contract: `@.codex/agents/researcher.toml`

Use only the canonical task envelope below. Distinguish verified evidence from inference and return findings to the Coordinator.

```json
{{canonical_envelope}}
```
