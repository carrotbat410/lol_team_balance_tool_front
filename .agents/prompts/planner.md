# Planner task template

Role contract: `@.codex/agents/planner.toml`

Use only the canonical task envelope below and the separately approved, sanitized Researcher handoff. Produce an approval-ready plan for the Coordinator.

```json
{{canonical_envelope}}
```
